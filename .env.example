# 연예인 추천 아이템 자동화 시스템 환경변수
# PRD v1.0 - 필수 API 키 및 설정

# =============================================================================
# API 키 설정 (필수)
# =============================================================================

# YouTube Data API v3 키
# https://console.developers.google.com/apis/credentials 에서 발급
YOUTUBE_API_KEY=your_youtube_api_key_here

# Google Gemini API 키
# https://makersuite.google.com/app/apikey 에서 발급
GEMINI_API_KEY=your_gemini_api_key_here

# Slack 웹훅 URL
# https://api.slack.com/messaging/webhooks 에서 생성
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Google Sheets ID (채널 목록 관리용)
# https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit 에서 ID 추출
GOOGLE_SHEETS_SPREADSHEET_ID=your_google_sheets_id_here

# =============================================================================
# 서버 포트 설정 (선택사항)
# =============================================================================

# API 서버 포트 (기본값: 8000)
API_PORT=8000

# 대시보드 포트 (기본값: 8501)
DASHBOARD_PORT=8501

# =============================================================================
# n8n 연동 설정
# =============================================================================

# RSS 수집용 API 서버 URL (n8n에서 사용)
RSS_AUTOMATION_API_URL=http://localhost:8000

# 채널 발견용 API 서버 URL (n8n에서 사용)
CHANNEL_DISCOVERY_API_URL=http://localhost:8000

# =============================================================================
# 디버그 설정 (선택사항)
# =============================================================================

# 디버그 모드 (개발 시에만 true)
DEBUG=false

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# 데이터베이스 설정 (선택사항)
# =============================================================================

# SQLite 데이터베이스 파일 경로 (기본값: data/candidates.db)
DATABASE_PATH=data/candidates.db

# =============================================================================
# AI 모델 설정 (선택사항)
# =============================================================================

# Whisper 모델 크기 (tiny, base, small, medium, large)
WHISPER_MODEL_SIZE=small

# 다운로드 디렉토리 (기본값: downloads)
DOWNLOAD_DIR=downloads

# =============================================================================
# 설정 가이드
# =============================================================================
#
# 1. 필수 설정:
#    - YOUTUBE_API_KEY: YouTube 영상/채널 정보 수집용
#    - GEMINI_API_KEY: AI 분석 파이프라인용
#    - SLACK_WEBHOOK_URL: 자동화 결과 알림용
#    - GOOGLE_SHEETS_SPREADSHEET_ID: 채널 목록 관리용
#
# 2. 시스템 시작 순서:
#    a) 환경변수 설정 (.env 파일 생성)
#    b) API 서버 실행: python src/api_server.py
#    c) 대시보드 실행: streamlit run dashboard/main_dashboard.py
#    d) n8n 워크플로우 임포트 및 활성화
#
# 3. 보안 주의사항:
#    - .env 파일은 절대 Git에 커밋하지 마세요
#    - API 키는 외부에 노출되지 않도록 주의하세요
#    - 프로덕션 환경에서는 더 강력한 인증 방식을 고려하세요
#
# =============================================================================